# Index - v0.x.x

import meta from './v0/_meta.json';
import { Cards, Card } from 'nextra/components';
import { CardsIcon, StarsIcon, ChevronRightIcon } from '../components/icons';

The following are all the docs sections for Pipeless versions `v0.x.x`

export const Index = () => {
    return (
        <Cards>
        {
            Object.keys(meta).map((section, key) =>
                <Card key={key} icon={<ChevronRightIcon />}
                    title={meta[section]} href={`/v0/${section}`}
                />
            )
        }
        </Cards>
    );
};

<Index />