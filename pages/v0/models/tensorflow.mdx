# Pipeless Tensorflow based Model Library

This is the collection of out of the box TensorFlow models.

## Install

To install the TensorFlow based library (and all the dependencies) simply run:

```bash copy
pip install pipeless-ai-tf-models
```