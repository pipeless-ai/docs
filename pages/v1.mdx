---
title: Pipeless v1.x
description: Documentation for Pipeless versions 1.x. The open-source computer vision framework
---

# Index - v1.x.x

import meta from './v1/_meta.json';
import { Cards, Card } from 'nextra/components';
import { CardsIcon, StarsIcon, ChevronRightIcon } from '../components/icons';

The following are all the docs sections for Pipeless versions `v1.x.x`

export const Index = () => {
    return (
        <Cards>
        {
            Object.keys(meta).map((section, key) =>
                <Card key={key} icon={<ChevronRightIcon />}
                    title={meta[section]} href={`/v1/${section}`}
                />
            )
        }
        </Cards>
    );
};

<Index />
